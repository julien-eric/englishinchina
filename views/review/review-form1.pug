h2.font-weight-light.border-bottom.border-gray.pb-2 Evaluate
#form-step-1(role='form', data-toggle='validator')
      .pb-5
      each criterium,i in criteria
            .pb-5
                  h4 #{criterium.code}. #{criterium.name}
                  p !{criterium.description}
                  .row
                        .slidecontainer.col-md-10.pt-1.range-field
                              input.form-form-1.slider(id='slider'+i name=criterium.label form="form-form-1" type='range', min='1', max='5', value='2')
                        .text-center.font-weight-light.col-md-2(id='ex'+(i+1)+'CurrentSliderValLabel') Score:
                              span.output(id='criteria'+i) 1
                              span /5

      h2.font-weight-light.border-bottom.border-gray.pb-2 Write about your experience 
            span.text-muted (optional)
      p.py-2 Is there anything else you wish to say about the school, or about the city in terms of how it affected your experience teaching abroad
      .form-group
            textarea.form-form-1(rows='8' form="form-form-1" name='comment' class='form-control' placeholder='Type your comment here')

      button.btn.btn-primary.btn-rounded.nextBtn.float-right(type='button') Next
      button.btn.btn-mdb-color.btn-rounded.prevBtn.float-right(type='button') Previous
