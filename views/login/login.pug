extends form-container
block card
    .col-md-10.col-sm-12.col-lg-6.order-2.order-md-1
        include value-prop

    .col-md-10.col-sm-12.col-lg-6.order-1.order-md-2
        .border.border-pale-gray.border-radius-5.text-dark.bg-white
            .my-5.text-left.px-3.px-md-5.py-1
                    .text-center
                            h3.mt-1.mb-5.font-weight-bold Sign in 
                    form(class='form-signin', action='/login', method='POST')
                            if(redirectUrl)
                                    .form-group.d-none
                                            input.form-control.form-control-lg(type='text', name='redirectUrl', placeholder='Username', value=redirectUrl)
                            .form-group.mb-5
                                    -var textInput = {}
                                    -textInput.id = 'email'
                                    -textInput.type = 'email'
                                    -textInput.name = 'email'
                                    -textInput.placeholder = 'Email'
                                    -textInput.label = 'Email'
                                    -textInput.required = true;
                                    include ../templates/inputs/text-input
                            .form-group.mb-5
                                    -var textInput = {}
                                    -textInput.id = 'password'
                                    -textInput.type = 'password'
                                    -textInput.name = 'password'
                                    -textInput.placeholder = '********'
                                    -textInput.label = 'Password'
                                    -textInput.required = true;
                                    include ../templates/inputs/text-input
                                    p.text-primary.text-small.forgot-password
                                            a(href="/forgot") Forgot your 
                                                    span.font-weight-bold password?
                            .text-center
                                    button.btn.btn-md.btn-block.btn-primary.px-5.px-5.mb-1(type='submit') Login
                            -var signupUrl = '/signup'
                            if(redirectUrl)
                                -signupUrl = signupUrl + '?redirectUrl=' + encodeURIComponent(redirectUrl)
                            p.mb-0( style="font-size: 0.8rem;")
                                    |Don't have an account yet?&nbsp;
                                    a(href=signupUrl) Sign up

            .footer.border-0.bg-light-secondary.text-dark.py-3.mt-5.text-left.px-3.px-md-5.pb-5
                    include social-login