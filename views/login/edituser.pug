extends image-splash
block card
    .card.text-dark.my-5
            .card-header.border-bottom-0
                .text-center
                    img(src='/images/logo.svg', width='', height='40', alt='')
            .card-body.text-left
                h3.text-center.mt-1.mb-3.text-light.font-weight-bold Edit 
                form(class='form-signin', action='/user/edit', method='POST')
                    .form-group
                        input#id(type="hidden", name="id", value=user.id)
                    .md-form
                        input.form-control(type='text', name='username', placeholder='Username', value=user.username, required, autocomplete="off", autofocus)
                        label(for='username') Username
                    .form-check.pl-0
                        -var isChecked = user.anonymous;
                        input#anonymous.form-check-input(checked=isChecked  type='checkbox' value='true' name="anonymous")
                        label.form-check-label(for='anonymous') Make me anonymous on across SLWorld
                        p.text-small.mt-1 This can be changed at a later time on your profile page. Keep in mind that reviews that you wrote with this setting on will still show your display name if you choose to turn off anonymous later on.
                    .text-center
                        button.btn.btn-primary.btn-lg.px-5.px-5.mb-1(type='submit') Save Changes