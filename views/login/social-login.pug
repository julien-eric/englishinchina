-var link = '/login/facebook'
if (redirectUrl)
    -link += '?redirectUrl=' + encodeURIComponent(redirectUrl);

.text-center.text-muted Social Login
  h1.form-group.mb-0
    a.btn.btn-fcb-blue.btn-md.btn-block.text-white.mdi.mdi-facebook-box(href=link)  Facebook
    //-   i.mdi.mdi-facebook-box(aria-hidden='true')